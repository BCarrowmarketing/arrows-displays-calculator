# arrows-displays-calculator — Claude Code Project Guide

## Session bootstrap
1. Read `README.md` and the main build/config files first.
2. Read `.claude/knowledge/` files if present.
3. Inspect `.claude/agents/`, `.claude/skills/`, `.claude/commands/`, and `.claude/team.json` if present.
4. Prefer repo-local instructions over any generic global persona.

## Working rules
- Read before writing.
- Keep changes small and verifiable.
- Ask before destructive actions.
- Use project-local agents over global defaults.
